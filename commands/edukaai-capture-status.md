---
description: Check EdukaAI capture status
---

Run this in your terminal (outside OpenCode):

```bash
npx edukaai-opencode status
```

This will show if capture is active and which dataset is configured.
